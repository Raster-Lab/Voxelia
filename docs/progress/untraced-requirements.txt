# Requirements in entered milestones traced by no record, source
# or test, recorded by ADR-0216 as an explicit debt baseline.
# Tools/Scripts/check_requirement_traceability.py fails when a row
# outside this list becomes untraced, so the debt can shrink but
# never grow. Remove a line when its row is genuinely traced.
#
# EMPTY as of 2026-08-06. The final thirteen rows were the VOX-CMP
# compression block; ADR-0255 opened that arc and names each one, so
# every requirement in an entered milestone is now traced by a record.
#
# TRACED IS NOT SATISFIED. ADR-0255 decision 1 splits the block: seven
# rows proceed as Voxelia-boundary work, and six -- VOX-CMP-004, 005,
# 006, 011, 012 and 014 -- are BLOCKED on an owner reconciliation,
# because they require characterising, benchmarking or adversarially
# testing the Raster-Lab codecs, which a standing owner instruction
# excludes. Being named in a record is what "traced" means here; it is
# not a claim that any of them is met.
