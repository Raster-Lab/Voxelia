# Requirements and traceability

Machine-readable indexes and derived requirement records belong here.

Validate the category, priority and milestone summaries against all normative rows and confirm that the checked-in traceability index is current:

```bash
python3 Tools/Scripts/generate_requirement_index.py --check
```
