# M0 acceptance workspace

Scaffold release v0.1.1 closes corrective action M0-001 and establishes the
Apple-only platform baseline. The available local build, test, resource, DocC
and SBOM checks pass on the approved Apple Silicon macOS host. Formal M0
acceptance still requires the unavailable visionOS destinations, external
repository-governance checks and human sign-off.

Corrective-release evidence is stored under `docs/releases/v0.1.1/`.
