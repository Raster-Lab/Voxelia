# Dataset fetch definitions

Large validation assets are content-addressed and fetched into an ignored cache.
A fetch record shall include URI, byte size, SHA-256 digest, licence and privacy
classification.
