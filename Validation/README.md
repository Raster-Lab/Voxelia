# Voxelia Validation Package

This internal package contains validation command-line tools, schemas,
controlled dataset manifests, expected results, tolerance profiles and reviewed
reports. It is not a public runtime dependency.

```bash
swift run --package-path Validation voxelia-validation --self-check
```


All execution and evidence generation is restricted to approved Apple Silicon macOS environments.
