# Voxelia Benchmark Package

This internal package contains correctness-gated benchmark scenarios, raw
result generation and approved baselines.

```bash
swift run --package-path Benchmarks voxelia-benchmark --self-check
```


All execution and evidence generation is restricted to approved Apple Silicon macOS environments.
